---
import { buildSearchIndex } from '../lib/search-index';
import { serializeSearchIndex } from '../lib/search';

const documents = await buildSearchIndex();
---

<script type="application/json" id="search-index" set:html={serializeSearchIndex(documents)} />
